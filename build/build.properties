#####################################################################
#You may require to change the following property(ies)...
#####################################################################
proj.root=D:/projects/git
proj.name=core
dist.dir=D:/projects/output/${proj.name}

#####################################################################
#You may change the following property(ies)...
#These are the messages that are displayed.
#####################################################################
msg.start=Initiating ${proj.name} build...

#####################################################################
#DO NOT CHANGE THE FOLLOWING PROPERTIES
#####################################################################
comp.name=iquesters

build.date.pattern=MMM-dd-yyyy
build.time.pattern=HH:mm:ss
build.user=${user.name}

src.dir=${proj.root}/${proj.name}/src
lib.dir=${proj.root}/${proj.name}/lib
setup.dir=${proj.root}/${proj.name}/res/setup
web.dir=${proj.root}/${proj.name}/web
res.dir=${proj.root}/${proj.name}/res
sql.dir=${proj.root}/${proj.name}/sql
server.config.dir=${proj.root}/${proj.name}/server-config